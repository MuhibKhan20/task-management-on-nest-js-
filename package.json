{
  "devDependencies": {
    "turbo": "^1.13.3"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "npm install --prefix=apps/backend && npm install --prefix=apps/frontend && cd apps/backend && npm run vercel-build && cd ../frontend && npm run build",
    "start": "node apps/backend/dist/main"
  },
  "workspaces": [
    "apps/*"
  ],
  "dependencies": {
    "chart.js": "^4.5.0",
    "react-chartjs-2": "^5.3.0",
    "superagent": "^9.0.0"
  }
}
