{
  "buildCommand": "npm install --prefix=apps/frontend && cd apps/frontend && npm run build && npm run vercel-build",
  "outputDirectory": "apps/frontend/dist",
  "functions": {
    "api/index.js": {
      "runtime": "nodejs18.x"
    }
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/index.js"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}